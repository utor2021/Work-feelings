<div class="jumbotron bg-light">
    <h1 class="display-4 text-center">Heres how your team has been feeling!</h1>
    <div class="container mt-4">
        {{#each users}}
        <div class="row">
            <p>
                {{first_name}} {{last_name}}
                {{#each statuses}}
                <br>{{emoji}}

                <br>
                {{created_at}}
                <br>
                {{diary}}
                {{/each}}
            </p>
        </div>
        {{/each}}
    </div>
</div>